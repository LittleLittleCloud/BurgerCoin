{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build docker img",
            "type": "shell",
            "command": "docker build . -t burgercoin:latest"
        },
        {
            "label": "test",
            "type": "shell",
            "command": "go test",
            "options": {
                "cwd": "${workspaceRoot}/src/test"
            }
        }
    ]
}